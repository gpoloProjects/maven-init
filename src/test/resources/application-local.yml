spring:
  config:
    activate:
      on-profile: local
  data:
    mongodb:
      host: localhost
      port: 27017
      database: testdb
      auto-index-creation: true
  mongodb:
    embedded:
      version: 6.0.11
